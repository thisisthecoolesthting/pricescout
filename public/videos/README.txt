# Walkthrough video

Drop **`walkthrough.webm`** here (`public/videos/walkthrough.webm`) — `/watch` references it via `<video>`.

Recommended: **1280×720** or **1920×1080**, VP9/WebM, ~60–90s, paired poster frame already falls back to `/images/app-interface.jpg`.
