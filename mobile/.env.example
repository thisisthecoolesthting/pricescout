# API base for the PriceScout backend.
# Dev: http://10.0.2.2:3300 (Android emulator's loopback to host machine running `npm run dev` on the web app)
# Prod: https://pricescout.pro
EXPO_PUBLIC_API_URL=https://pricescout.pro

# Optional: app environment label shown in dev builds.
EXPO_PUBLIC_APP_ENV=production
