# Reply — PRICESCOUT-DUAL-SURFACE-COPY-002

## Done

- **Home (`/`):** Dual-surface hero subhead, three-item `TrustStrip`, post-CTA line, two-up hero grid (`hero-phone-mockup.jpg` + `hero-laptop-webcam.jpg`), “What is” copy mentions four installs phones+browsers, home “How it works” step 1 mentions `/scan` in browser, dark CTA strip aligned to dual surfaces.
- **`/scan`:** Metadata + intro paragraph + chip strip (Browser webcam / Phone Android / iOS in review).
- **`/pricing`:** Per-tier **4 scanner installs** line in `PricingTiers`; new **Scanner installs** FAQ block (two questions) via existing `FAQ` accordion.
- **Industries:** `thrift-stores`, `estate-sales`, `yard-sales`, `flea-markets` subtitles + shared three-line trust strip (all six industry pages use the same dual-surface trust line for consistency).
- **`/how-it-works`:** Restructured to Snap (phone vs browser columns) → Verdict → Save.
- **`/features/browser-scanner`:** New feature slug in `feature-pages.ts`; hub lists it automatically; links added from Visual identification, Shared flip log, and Device management related blocks.
- **Asset:** `public/images/hero-laptop-webcam.jpg` added (session-generated scene per dispatch B; replace with Leonardo pipeline if you want pixel-perfect brand control).

## Visual diff

Not bundled as image files in-repo. Recommended manual pass: compare `/` hero and `/scan` header in dev or staging before merge.

## Proof

See `build/proof/PRICESCOUT-DUAL-SURFACE-COPY-002.json`.
